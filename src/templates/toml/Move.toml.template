[package]
name = "{{ package_name }}"
version = "0.0.1"

[dependencies]
Sui = { git = "https://github.com/MystenLabs/sui.git", subdir = "crates/sui-framework/packages/sui-framework", rev = "framework/{{ environment }}" }

[addresses]
{{ package_name }} = "0x0"
